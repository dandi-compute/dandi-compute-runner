name: Archive job

permissions:
  contents: read
  actions: read

on:
  workflow_dispatch:
    inputs:
      status:
        description: "Archive every job capsule with this status. Combine with `pipeline` to narrow it to one pipeline."
        required: true
        type: choice
        default: none
        options:
          - none
          - failed
          - pending
          - stalled
      pipeline:
        description: "Archive every job capsule of this pipeline, whatever its status unless `status` is also set."
        required: true
        type: choice
        default: none
        options:
          - none
          - aind+ephys
          - lfp
      path:
        description: "Path of a single job capsule folder, relative to the job capsules Dandiset root. Only used when `status` and `pipeline` are both `none`."
        required: false
        type: string

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  DANDI_API_KEY: ${{ secrets.DANDI_API_KEY }}
  DANDI_DEVEL: ${{ secrets.DANDI_DEVEL }}
  DANDICOMPUTE_OOP_FAILSAFE_LOG: ${{ vars.DANDICOMPUTE_OOP_FAILSAFE_LOG }}

jobs:
  CheckEngaging:
    name: Pre-run Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Cancel if offline
        uses: ./.github/actions/check-runner
        with:
          labels: mit,engaging,submitter
          token: ${{ secrets._GITHUB_API_KEY }}

  ArchiveJob:
    needs: CheckEngaging
    name: Archive job
    runs-on: [self-hosted, mit, engaging, submitter]
    # Each step records itself in the global logs repository (see launcher/record.sh); the shell
    # after `--` is GitHub's own default for bash.
    defaults:
      run:
        shell: /orcd/data/dandi/001/dandi-compute/dandi-compute-runner/launcher/record.sh logs archive-job -- bash --noprofile --norc -eo pipefail {0}

    steps:
      - name: Ensure DANDI CLI is kept up-to-date
        working-directory: /orcd/data/dandi/001/dandi-compute
        run: pip install -U dandi

      - name: Archive job
        working-directory: /orcd/data/dandi/001/dandi-compute
        env:
          STATUS: ${{ inputs.status }}
          PIPELINE: ${{ inputs.pipeline }}
          CAPSULE_PATH: ${{ inputs.path }}
        run: |
          if [ "$STATUS" = "none" ] && [ "$PIPELINE" = "none" ]; then
            if [ -z "$CAPSULE_PATH" ]; then
              echo "::error::Provide a job capsule \`path\`, or choose a \`status\` and/or \`pipeline\` to archive."
              exit 1
            fi
            dandicompute archive --job "$CAPSULE_PATH" --base /orcd/data/dandi/001/dandi-compute
          else
            if [ -n "$CAPSULE_PATH" ]; then
              echo "::error::\`path\` must be empty when archiving by \`status\` ($STATUS) or \`pipeline\` ($PIPELINE)."
              exit 1
            fi
            FILTERS=()
            if [ "$STATUS" != "none" ]; then
              FILTERS+=(--status "$STATUS")
            fi
            if [ "$PIPELINE" != "none" ]; then
              FILTERS+=(--pipeline "$PIPELINE")
            fi
            dandicompute archive "${FILTERS[@]}" --base /orcd/data/dandi/001/dandi-compute
          fi

  NotifyOnFailure:
    if: ${{ always() && failure() }}
    needs: [CheckEngaging, ArchiveJob]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Check 24-hour cooldown
        id: cooldown
        uses: ./.github/actions/check-cooldown
        with:
          workflow_file: archive-job.yml
          token: ${{ github.token }}

      - name: Send email notification on failure
        if: ${{ steps.cooldown.outputs.should_notify == 'true' }}
        uses: dawidd6/action-send-mail@94de994a9f6fffee200243214e17002e2920bb59 # v18
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "DANDI Compute: job archiving failed (MIT Engaging dispatch)"
          to: cody.c.baker.phd@gmail.com  # Add more with comma separation (no spaces)
          from: DANDI Compute <dandi-compute@users.noreply.github.com>
          body: |
            The manual dispatch of job archive encountered an issue!
            Please check status at: https://github.com/dandi-compute/dandi-compute-runner/actions/workflows/archive-job.yml
